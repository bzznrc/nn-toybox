# trace

Inference paths through a tiny MLP trained on the bundled zipped 8x8 digits.

Default display: `python -m nn_toybox.display --demo trace`

Headless run: `python -m nn_toybox.run --demo trace --steps 1000`
