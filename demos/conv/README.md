# conv

Learned convolution filters and feature maps on the bundled zipped 8x8 digits.

Default display: `python -m nn_toybox.display --demo conv`

Headless run: `python -m nn_toybox.run --demo conv --steps 1000`
